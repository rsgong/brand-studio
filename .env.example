# Supabase — create a project at https://supabase.com/dashboard
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# n8n webhook base URL (where your generation workflows live)
VITE_N8N_WEBHOOK_URL=https://your-n8n-instance.app/webhook
VITE_N8N_PASSPHRASE=your-passphrase-here

# n8n webhook path IDs — these are generated when you create/import the workflows
# Find them in n8n: open the workflow → click the Webhook node → copy the path
VITE_N8N_IMAGE_WEBHOOK_PATH=your-image-webhook-uuid-here
VITE_N8N_VIDEO_WEBHOOK_PATH=video-generate-v2
